let fs,checkFileExists;_67d‍.x([["createFile",()=>createFile]]);_67d‍.w("fs",[["default",["fs"],function(v){fs=v}]]);_67d‍.w("./utils/checkFileExists",[["checkFileExists",["checkFileExists"],function(v){checkFileExists=v}]]);


       const createFile = async ({ name, path, hasFolder, ext }) => {
  const dir = `${path}${name}`;
  if (hasFolder) {
    await fs.mkdir(dir, { recursive: true }, () => {});
  }
  const filePathName = `${dir}/${name}${ext}`;
  const fileExists = await checkFileExists(filePathName);
  if (!fileExists) {
    fs.writeFile(filePathName, 'test2', () => {});
  } else {
    console.log('it exists')
  }
}